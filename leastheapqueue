import heapq
import sys
N = int(sys.stdin.readline())
heap=[]

for order in range(N):
    ctr = int(sys.stdin.readline())
    if ctr == 0:
        if len(heap)==0:
            print(0)
        else:
            print(heapq.heappop(heap))
    else:
        heapq.heappush(heap,ctr)
    

                
        
